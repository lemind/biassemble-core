# Audit-mode golden sets

Three golden sets for the two audit-mode prompts (EXTRACT, VERIFY) and the code-side numeric normalization layer, per ADR-000 §4 steps 2–3 and D018 §2.1–§2.3. All built against one real, unmodified SEC filing — see `source-filing.md` — **with deliberate, noted exceptions**: `verify-golden-set.json` cases 21–29 pin two real production incidents against real legal-contract text (the Stock Transfer Agreement used in actual `app.gc.ai`/`app.openbb.co` audit runs, 2026-07-23/26), and cases 30–33 pin a third real incident against real text from a different company's public filing (Allogene Therapeutics 10-K/8-K, `app.openbb.co`, 2026-07-28) — none of this is the canonical Apple filing. Justification: these are verbatim reproductions of real, cross-domain/cross-company target-run failures — a fix can only be checked deterministically against the exact claim/passage pair that broke, not a same-domain analog, per this file's own labeling discipline below. Every other case in both golden sets (1–20 in verify, all 12 in extract) still traces to `source-filing.md`.

This is deliberately **synthetic-equivalent data**: the ten EXTRACT texts are hand-authored "AI research note" style paragraphs about a real filing, not any company's actual product output. They are for measuring the pipeline, never for making claims about Apple. The pilot-target teaser material (ADR-000 §4 step 6) is a separate, later step that uses a target company's own public output — see `context-prompt-b2b-transformation.md` §8 for that runbook.

## Labeling discipline (read before extending any of these files)

**Labels are written before the prompt they test exists to run against them.** Every `expected_claims`, `expected_verdict`, and `expected` block in these three files was hand-authored at the same time as the input it labels — the same person who wrote a trap decided its correct answer before any model ever saw it. This is not a formality: `hypotheses/v2.yaml` in the engine repo was tuned while looking at eval failures by name, and the Marta test exists specifically because that tuning wasn't out-of-sample. Do not repeat that mistake here.

If you need a new case: write the input and its expected answer together, commit them, *then* run the prompt. If a prompt run reveals a case was mislabeled, fix the label with a comment explaining why, don't silently adjust it to match what the model produced.

## Test-count discipline (added 2026-07-29, after a review found 4 tests that proved nothing)

Every golden case gets a pinned regression test — that policy stands. But **do not add a standalone test that only asserts a golden-set array's `.length` against a hardcoded number** (e.g. `it("covers all N scenarios", () => expect(set.length).toBe(N))`). Four of these existed across `audit-verify.test.ts`, `audit-extract.test.ts`, `compare.test.ts`, and `derive.test.ts` and were removed on review: none of them ever called the code under test (`VerifyService`, `ExtractService`, `compare()`, `derive()`) — they only checked that a JSON file's array length matched a number restated from this README's own table above. Their only real function was flagging when someone added/removed a case without updating the number, which:
- an aggregate pass-bar test (where one exists, e.g. `audit-verify.test.ts`'s "meets the ≥36/37 pass bar in aggregate") already does for free as part of asserting something that actually matters, and
- where no aggregate test exists, is just as well caught by this table (**Cases** column) and doesn't need its own test slot.

The rule going forward: the case **count** lives in this README's table (and the quickstart pass bar, where applicable) — not in a dedicated assertion. A new golden case still needs its own `it(...)` exercising real behavior; it does not also need the total to be re-asserted separately.

## Seed-vs-expected discipline (added 2026-07-29, after a review found 3 golden cases that couldn't fail)

`audit-verify.test.ts` seeds `MockProvider`'s raw LLM response from each scenario's own `expected_verdict`/`expected_evidence` — for 37 of the 41 cases, that's correct: they pin "does the service correctly persist/shape a response VERIFY already got right," per this file's honest, long-standing limitation (no case here makes a real, budget-affecting model call). But a golden case pinning a **code-side reconciliation fix** needs the mock to emit the model's actual *wrong* raw output, then assert the service corrects it — seeding it with the already-correct answer instead means the case passes identically whether the fix exists or not. Cases 38/39/40 hit exactly this: all three originally seeded `verdict: expected_verdict`, so none of them actually exercised `reconcileNumericVerdict`'s removed gate, `reconcileTemporalVerdict`, or `reconcileDefinedTermVerdict`'s override branch (verify-040 was worst — its seeded `partially_supported` hit that function's own early-return guard before any new logic ran at all).

Fixed by adding three optional fields to the golden-case schema: `seed_verdict`, `seed_evidence`, `seed_confidence` — when present, these seed the mock's raw response instead of the `expected_*` fields, while the assertions still always check the *persisted* result against `expected_verdict`/`expected_evidence`. Cases 38/39/40 now seed the actual wrong raw verdict/confidence each real incident produced (`unverifiable`@0, `supported`@1, `supported`@0.85 respectively) and assert the service corrects each one — these would fail against the pre-fix code, unlike the versions that shipped first. Use `seed_*` fields for any future case pinning a code-side correction; omit them (as all other cases do) when the case is testing correct-response persistence instead.

## The three sets

| File | Cases | Tests |
|---|---|---|
| `extract-golden-set.json` | 12 | EXTRACT prompt (D018 §2, `context-prompt-b2b-transformation.md` §4) — case 11 (dedup) added during `specs/008-b2b` task-review, spec.md's own edge case had zero coverage until then; case 12 (adjacent sub-reference) added after a real production incident (2026-07-23): EXTRACT paraphrased "Article 19-2" down to "Article 19" in the same sentence as a bare "Article 19", producing a false "the audited output got this wrong" verdict on a claim it stated correctly — also the first case to exercise the `entity` claim type |
| `verify-golden-set.json` | 40 | VERIFY prompt (D018 §2.3, `context-prompt-b2b-transformation.md` §5) — case 16 (derived-ratio magnitude miss) added after a real production incident (2026-07-22): VERIFY computed a correct ratio in its own note but emitted `supported` instead of `contradicted`; case 17 is the partial-credit boundary condition for the same fix (synthetic numbers, not from the source filing); cases 18/19 (inference tolerance) added after a real production incident (2026-07-23): a negative claim wrongly marked unsupported despite a necessarily-precluding fact in evidence, and an adjacent-but-unstated claim wrongly marked supported — both encode the same unifying inference-tolerance rule added to the VERIFY prompt (v2.2.0), in opposite directions; case 20 added after a second, more severe real production incident (2026-07-26, app.openbb.co): a compound negative claim was marked `contradicted` (not just `unsupported`) with a note that itself argued for support — closed two gaps in v2.3.0, the `contradicted` bullet's missing entailment carve-out and a new VERDICT/NOTE CONSISTENCY instruction; cases 21–29 (v2.4.0), same 2026-07-26 run re-tested against v2.3.0 — 21/22 pin the two real arbitration repros verbatim (a deliberate exception to this file's one-SEC-filing scope, see note below), 23–26 pin a real synonym-recognition regression (`encourage` vs `solicit` on one clause got 3 different verdicts across 4 identically-structured claims), 27–29 pin a real redacted-placeholder-value policy that had drifted across three separate runs of the same document; cases 30–33 (v2.5.0) added after a third real production incident (2026-07-28, app.openbb.co, Allogene Therapeutics 10-K/8-K): four structurally-parallel causal claims split from one "depends on A, B, C, D" sentence, verified against the same 2-passage pool, got four different verdicts (unverifiable@0/unverifiable@0/unsupported@0.7/supported@0.7). First-pass diagnosis wrongly assumed all four must resolve identically — corrected on review: only 2 of the 4 items (financing, clinical progress) have real topical overlap with the retrieved passages, the other 2 (expense discipline, regulatory outcomes) have none at all, so verify-030/031 correctly expect `unsupported` and verify-032/033 correctly expect `partially_supported`, not a single forced verdict. Fix is a consistent per-item decision standard (confidence-calibration guidance on rule 3, plus a PARALLEL CLAIMS instruction requiring the same standard per item, explicitly not the same verdict per item); case 34 added after a fourth real production incident (2026-07-29, same run): a going-concern claim flipped between runs (supported@0.9 vs unsupported@0.7) on a passage that's on-topic (sustained losses, financing need) but never uses the specific term — a second real confirmation of the same rule-3 pattern, no prompt change needed beyond what v2.5.0 already added; case 35 (v2.6.0) added after reverting the unsafe keyword-proximity numeric fallback — a fresh real run showed VERIFY's own note identifying the exact conflicting same-measure figure and still returning unsupported/unverifiable at confidence 0 instead of contradicted; VERDICT/NOTE CONSISTENCY extended to require the structured verdict/evidence/confidence match what the note already concluded, in both the agreement and conflict directions, generalized beyond numeric claims to entity claims too; cases 36/37 added after a fifth real production incident (2026-07-29, same run, batch size lowered 10→8): 36 pins a code-side bug, not a prompt bug — `reconcileNumericVerdict`'s evidence-fact extraction took the first number in a multi-figure evidence sentence (net loss aggregate) instead of the one comparable to the claim (per-share loss), hit a scale mismatch, and left a false `supported` unreconciled; fixed by disambiguating among all numbers in the evidence text via scale-presence rather than trusting the first match. 37 pins the same run's two-segments claim, still self-contradicting (note says "contradicts", verdict said `unsupported`/`supported` across different runs) — an entity claim the numeric checks can't reach at all; fixed by a new code-side `reconcileVerdictNoteConsistency` check, independent of any numeric extraction, that rejects `supported` whenever the note itself asserts a contradiction. Note: the same run's cash-runway claim (Q3 2026 claimed vs Q1 2028 actual) flipped to a *self-consistent but substantively wrong* `supported` — verdict and note agree with each other, they're just both wrong about the entailment — which neither of these two fixes can catch; that failure needs a real temporal comparator, deliberately not attempted here, and remains an open gap; cases 38–40 (2026-07-29, a live deployed run on run-9's own pipeline_code_version, confirmed against 3 consecutive real runs) close three gaps found in that run: 38 makes `reconcileNumericVerdict` run unconditionally on every raw verdict shape (not just supported/contradicted) so a claim landing on `unverifiable`/`unsupported` with real disagreeing evidence still gets corrected — the exact gap 36/37 didn't close, since those only ever exercised the `supported`-input direction; 39 is the temporal comparator the same README entry above called "deliberately not attempted" — a cash-runway claim naming Q3 2026 against evidence naming Q1 2028 was marked `supported` on the reasoning that a later date "supports" an earlier one, reproduced identically across 3 runs, fixed with a code-side quarter/year comparator, same bidirectional shape as the numeric checks; 40 gives the going-concern instability (case 34) a deterministic code-side floor — the same claim/passage pair bounced between `supported`@0.8–0.9 and `unverifiable`@0.7 across repeated real runs on identical code, so a short curated defined-term list (starting with "substantial doubt") now forces `partially_supported` whenever the term is claimed but absent, verbatim, from evidence, regardless of which way the model guessed. Numeric and temporal overrides also set confidence to 1 — found on review that `GateService` runs after `VerifyService` and force-resets anything below the confidence threshold back to `unverifiable` (wiping evidence to null), so a verdict override that didn't also clear that bar would get silently undone one pipeline stage later, which is exactly why four claims in the run-9 curl dump stayed at `unverifiable`@0 despite their own notes naming the real conflicting figures. A subsequent high-effort code review of cases 38–40's own implementation (same day) found and fixed four further issues before any of it shipped: (1) chaining five reconcilers sequentially, each fed the previous one's output, let a LATER reconciler silently undo an EARLIER one's correct override for an unrelated reason (e.g. a confirmed $640M-vs-$64M contradiction flipped back to `supported` because an unrelated matching quarter mention was also present) — fixed with a shared `CODE_OVERRIDE_TAG_RE` guard every reconciler checks first, deferring to any prior stage's already-tagged decision; (2) `reconcileTemporalVerdict`, unlike currency, had no same-measure safety signal for bare quarter+year tokens, so it was scoped back to `supported`/`contradicted` raw verdicts only (every real incident behind it already had raw verdict `supported`, so nothing was lost); (3) `reconcileDefinedTermVerdict` had no negation guard (a claim asserting the ABSENCE of "substantial doubt" still matched) and no going-concern anchor (the bare phrase could misfire in unrelated litigation/analyst-commentary claims) — both fixed; (4) `reconcileVerdictNoteConsistency` and `reconcileDefinedTermVerdict` no longer force confidence to 1 on override — both are regex/text heuristics, not proven arithmetic, so they deliberately stay behind `GateService`'s confidence gate as a real safety net, unlike the three deterministic comparators. A back-to-back A/B run on identical code (2026-07-30) confirmed the temporal comparator fires deterministically every time, and surfaced one more real bug across all 7 override sites: every one of them appended its code-tag AFTER whatever note the model wrote, so a corrected `contradicted` verdict could sit right next to the model's own original sentence arguing the opposite ("...which supports the claim...") — confusing at best, a trust-killer in a customer-facing report at worst. Fixed with a shared `buildOverrideNote()` helper: the code's conclusion now always leads, with the model's original note (if any) kept but clearly parenthetically marked "superseded by the above," never blended in a way that reads as the current explanation. The same A/B run also reconfirmed the numeric comparator's evidence-presence gate (not a verdict gate — that part is already unconditional) is still the one open, deliberately-unsolved risk: net loss stayed `unsupported` in both runs while EPS/assets/liabilities flipped to `contradicted` in one but not the other, consistent with the model populating `evidence` non-deterministically per claim per run rather than any inconsistency in the deterministic comparison logic itself — not yet independently confirmed against raw per-run evidence/note data, flagged as the next thing to verify before touching code again |
| `numbers-golden-set.json` | 20 | Code-side normalization layer between EXTRACT and VERIFY (D018 §2.3, "arithmetic happens in code, never in the LLM") |

## EXTRACT match rule (there was none before this file — defining it here)

A model-extracted claim **matches** an expected claim iff:
1. `type` is identical, and
2. `excerpt` matches the expected excerpt after normalization (lowercase, collapse whitespace, unify quotes/dashes/ellipses — same normalization `grounding-numbers-plan.md`'s excerpt-verification phase already specifies for the evidence-quote check) — exact match, or fuzzy ≥95 (rapidfuzz `partial_ratio`) on the fallback, and
3. the `claim` field's stated value/entity/direction doesn't contradict the expected claim (paraphrase in wording is fine; the excerpt anchor and the type must line up).

Score per scenario: **precision** = matched / total extracted, **recall** = matched / total expected. Aggregate across all 10.

**Pass bar**: recall ≥ 0.90, precision ≥ 0.85, and **zero leaks from any scenario's `excluded_content`** — extracting an opinion, forecast, or hedge as if it were a checkable claim is an automatic fail for that scenario regardless of the P/R numbers, since that's the exact failure the exclusion rules in the EXTRACT prompt exist to prevent.

`locations` (every expected claim now carries one, e.g. `["p1s2"]`, sentence-position within `output_text`) is recorded and worth eyeballing during review, but is **not** part of the pass/fail score above — no fuzzy-matching tolerance has been designed for it yet. Score on type + excerpt only until that's decided.

VERIFY doesn't need an equivalent rule — its output is an enum (`supported | partially_supported | unsupported | contradicted | unverifiable`), so "did it match" is just "is it the same string," per D018 §2.3.

## Numbers-layer pass bar

**Zero false "not comparable = contradicted."** Whenever `comparable: false` (different currency with no FX rate, missing unit, period ambiguity, pp-vs-% confusion), the case must never be treated downstream as evidence the claim is wrong — a data gap is not a contradiction. This is called out because it's the one failure mode that accuses a customer's AI of an error it didn't make, which is fatal to the product's credibility on its own terms (ADR-000 §2's headline promise is FP discipline). Every `num-0XX` case with `comparable: false` in `numbers-golden-set.json` exists specifically to catch a normalizer that guesses instead of flagging.

## What's still missing

**EXTRACT's attribution instruction is unimplementable as written.** The prompt says, for attribution claims: "mark the inner content separately if itself checkable" — but the output schema has no field for it (no second claim, no link between an attribution claim and its inner-content claim). None of the EXTRACT cases here test it, because there's nothing to test yet. Needs a decision before EXTRACT is wired: either cut the instruction, or add the schema field (e.g. `inner_claim_ref`) and a golden case exercising it. Not decided here.

~~`entity` claim type (numeric/entity/attribution/causal/derived per the EXTRACT prompt) is never exercised~~ — closed by case 12 (`extract-012-adjacent-sub-reference`).

There is no dedicated numeric-claim-verification spec document in this repo — `context-prompt-biassemble-overview.md` (~/Downloads) §8 references one under the name `prompt-plan-grounding-numbers.md`, but the file that actually exists under a similar name (`grounding-numbers-plan.md`) is about excerpt-verification/calibration/canary discipline, not unit/currency/period normalization. `numbers-golden-set.json` above is built directly from the one-line principle in `b2b-change-plan.md` and D018 §2.3, not from a fuller spec — because no fuller spec exists on disk anywhere. Worth writing one before this golden set is treated as complete/final rather than a reasonable first draft.

## Source filing provenance

Apple Inc. Form 10-Q, quarterly period ended March 28, 2026, filed 2026-05-01. Public EDGAR filing, fetched 2026-07-20 via `curl` with an SEC-compliant User-Agent header (SEC requires a declared contact identifier, not a login — no ToS issue). Full excerpt and location-tag index: `source-filing.md`.
